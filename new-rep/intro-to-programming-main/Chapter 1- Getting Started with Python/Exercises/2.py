#Print the version of Python

#The 'import sys' is used to add the Python sys module, which prints the Python version in use.
import sys

#Print the string word 'Python version'
print("Python version")

#Print the 'sys.version' 
print(sys.version)